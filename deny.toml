# cargo-deny configuration
# https://embarkstudios.github.io/cargo-deny/
# Run: cargo deny check

[graph]
all-features = true

[advisories]
# Ignore specific advisories by ID
ignore = []

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "Zlib",
    "CC0-1.0",
    "OpenSSL",
    "BlueOak-1.0.0",
    "MPL-2.0",
    "CDLA-Permissive-2.0",
    "BSL-1.0",
]

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"
deny = []
# Allow workspace path dependencies (treated as wildcards by cargo-deny)
allow-wildcard-paths = true

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
