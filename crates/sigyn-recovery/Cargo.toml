[package]
name = "sigyn-recovery"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "sigyn-recovery"
path = "src/main.rs"

[features]
vendored-openssl = ["sigyn-engine/vendored-openssl"]

[dependencies]
sigyn-engine = { path = "../sigyn-engine", version = "0.15.0" }

# CLI
clap.workspace = true
rpassword.workspace = true
dialoguer.workspace = true
console.workspace = true

# Error handling
anyhow.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
# Needed to write `.identity` files in the exact format the CLI's identity
# store reads (CBOR body + BLAKE3 keyed MAC) — must match sigyn-engine.
ciborium.workspace = true
blake3.workspace = true

# Other
chrono.workspace = true
directories.workspace = true
git2 = "0.20"

[dev-dependencies]
tempfile.workspace = true
